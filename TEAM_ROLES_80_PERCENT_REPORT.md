**DEVANSHI: Frontend User Interface & Interactive UX Engineering**
Responsible for web architecture, Jinja2 templating, file capture mechanics (`enctype="multipart/form-data"`), and CSS design systems across `templates/` (`index.html`, `student.html`, `farmer.html`, `widower.html`, `result.html`) and `static/style.css`. Devanshi structured accessible HTML5 forms capturing complex criteria (`age`, `income`, state, caste, and land acreage) alongside multi-file upload pipelines (`name="documents" multiple`). In the 80% deliverable update, she integrated direct clickable government portal gateways (`apply_link`) into dynamically rendered scheme cards so users can immediately launch official application forms. Additionally, she implemented conditional CSS rules (`display: none;`) to keep image preview containers hidden until files are selected, and enforced UTF-8 multi-script character encoding across all templates for flawless bilingual rendering of Gujarati, Hindi, and English UI labels. In the final update, she expanded the UI by introducing the "Others" and "Girl Child" persona forms (with dynamic JS toggles for conditionally displaying fields like Disability Type) and integrated the floating AI Chat widget (`chat_widget.html`) with responsive design and `.pulsing` CSS animations.

**OM: Core Backend Engine, Multi-Rule Masking & Automated Verification**
Responsible for the Flask WSGI server core, HTTP routing (`@app.route`), Pandas vectorized engineering, and automated regression suites across `app.py`, `schemes.csv`, and `test_eligibility_engine.py`. Om mapped form payloads to Python dictionaries (`request.form.to_dict()`) and securely handled incoming document byte streams via `secure_filename()`. Utilizing Pandas, he sanitized missing CSV entries (`.fillna()`) and applied vectorized Boolean masking (`age_mask`, income ceilings) to evaluate citizen profiles against government datasets in milliseconds. In the 80% deliverable update, Om upgraded the rule engine to parse compound multi-caste constraints (`SC/ST`), expanded database schemas with verified portal links, and engineered an automated 30-profile regression verification suite that validates boundary income thresholds, land rules, and BPL checks with 100% precision. In the final update, Om restructured the `schemes.csv` database schema for sub-categories and gender-based masking, integrated `.env` configuration via `python-dotenv`, and expanded the backend Pandas evaluation engine to support the new personas.

**DEV (Leader): Computer Vision, 360° Auto-Orientation & Multi-Script OCR**
Responsible for OpenCV image preprocessing pipelines, 360° orientation computer vision, multilingual Tesseract LSTM engines, and script detection across `app.py` (`preprocess_image`, `auto_orient_image`, `evaluate_ocr_confidence`, `detect_doc_language`, `to_english`). Dev implemented image decoding (`cv2.imdecode`), adaptive thresholding, and neural net flags (`--oem 3 --psm 4 -l eng+guj+hin`) to simultaneously parse English, Gujarati, and Hindi characters. He detected document languages natively via Unicode character blocks (`[\u0A80-\u0AFF]`). In the 80% deliverable update, Dev engineered a 360° automatic orientation detection booster (`auto_orient_image()`) that evaluates keyword/number anchors across 0°, 90°, 180°, and 270° angles to automatically rotate sideways or upside-down photos upright before OCR, alongside resolution upscaling and bilateral edge-preserving denoising. In the final update, Dev spearheaded the integration of the Google Gemini API to power the SahayakAI Assistant, engineered the precise `SYSTEM_PROMPT` bounding the LLM to scheme-related queries in simplified Devanagari Hindi, implemented conversational memory payload passing, and engineered the Web Speech API architecture for the Hindi Voice Assistant natively inside the chat widget (using `window.SpeechRecognition` and `window.speechSynthesis`).

**ARYAN: Structural Anchor Parsing, Regional NLP Extraction & PDF Engine**
Responsible for structural anchor regex engineering, regional script translation handlers, agricultural unit conversions, and ReportLab PDF generation across `app.py` document extractors (`extract_aadhaar_data`, `extract_income_certificate_data`, `extract_land_document_data`, `extract_death_certificate_data`, `extract_bpl_card_data`). Aryan wrote regex masks to protect Aadhaar privacy while capturing last-4 digits, normalized dates (`DOB / Date of Birth / જન્મ તારીખ`), and converted diverse land units (Hectares, Guntas, Bighas) into Acres (`_to_acres`). In the 80% deliverable update, Aryan engineered a DOB-anchored smart name extraction algorithm that locates Date of Birth anchor lines to accurately extract names directly above them, integrated automated regional script-to-English translation (`to_english`), and added automatic Gujarati script-to-state fallback inference.
